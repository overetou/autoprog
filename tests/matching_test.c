//This is not a source file but a test file, only meant to be parsed during tests.

char	*hello(void)
{
	printf("hello people! A sneaky func()");
	free(everything[4]);
	main();
}

int		thefuncthatexpandontreelines(
	int hello, char c
)
{
	if (c)
		hello = 5;
	return hello;
}

int main(int argc, char const *argv[])
{
	launch_flowers(get_flowers, 5 * count_flowers);
	thefuncthatexpandontreelines(6, 1);
	return 0;
	main();
}
